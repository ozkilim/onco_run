# Copy to `.env` and edit the paths. Used by docker-compose.yml.
SLIDES_DIR=/absolute/path/to/wsi/folder
OUTPUT_DIR=/absolute/path/to/output/folder
RECIPE_PATH=./recipes/example.yaml
MODELS_DIR=./models
PREDICTORS_DIR=./predictors
